name: sermon-dark
description: House style — white Palatino text on dark background (8 example decks)
source_example: 100_fold_blessing
extends: default.yaml
slide_size: widescreen
slide_style:
  text_color: white
  alignment: left
  reference_position: bottom
  font_name: Palatino
  # Defaults when unset: highlight orange, reference grey-on-dark, title white
  # Optional layout SDK (uncomment to override geometry / sizes):
  # typography:
  #   body_size_pt: 32
  #   reference_size_pt: 28
  # layouts:
  #   verse:
  #     margin_in: 0.6
  #   title:
  #     title_top_in: 2.5
