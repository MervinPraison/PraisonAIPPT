---
name: ppt-yaml-deck-workflow
description: Edit PraisonAIPPT YAML decks in examples, keep style consistent, regenerate matching PPTX, and verify output. Use when users request slide/verse/section/highlight updates for deck YAML files.
disable-model-invocation: true
---

# PPT YAML Deck Workflow

## Scope

Use this workflow for deck edits in `examples/*.yaml` where the output is a `.pptx` generated by `praisonaippt`.

## Standard checklist

Copy and track:

```text
Progress:
- [ ] 1) Identify target YAML deck and exact requested changes
- [ ] 2) Read current YAML structure before editing
- [ ] 3) Apply minimal YAML edits only
- [ ] 4) Regenerate matching PPTX with CLI
- [ ] 5) Confirm build success and Drive upload link
- [ ] 6) Commit/push YAML updates when requested
```

## 1) Identify deck file

- Confirm the target YAML in `examples/`.
- If user gives only topic/title, create a new YAML in `examples/` with the requested name.

## 2) Read before edit

- Read the YAML section where changes are needed before modifying.
- For reorder requests, verify both source and destination blocks first.

## 3) Apply minimal edits

- Keep existing `slide_style` unless explicitly asked to change.
- Preserve verse text unless the user asked to rewrite it.
- For highlights, prefer keyword-only highlights unless user asks otherwise.
- For section title slides, add a new `section` block only where needed.

## 4) Regenerate PPTX (required after YAML edit)

From repo root:

```bash
python3 -m praisonaippt.cli -i examples/<deck>.yaml -o examples/<deck>.pptx
```

Example:

```bash
python3 -m praisonaippt.cli -i examples/100_fold_blessing.yaml -o examples/100_fold_blessing.pptx
```

## 5) Verify result

- Ensure CLI reports: `Presentation created successfully`.
- Capture Drive output (`View Link`) when present.
- If YAML parse fails, fix quoting/block-scalar issues and rerun immediately.

## 6) Git update

- Stage only intended files.
- Commit with a clear one-line message.
- Push the current branch.

## Common YAML pitfalls

- Plain scalars containing `:` may require quotes.
- Keep list indentation consistent under `sections` and `verses`.
- Use block scalars (`>-` or `|-`) for multi-line verse text.
